#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/your_db_name
#    username: your_username
#    password: your_password
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
spring:
  datasource:
    url: jdbc:h2:file:/home/tony/Projects/photo-gallery-cms/database/photodb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

file:
  upload-dir: ${user.dir}/persistent_uploads
